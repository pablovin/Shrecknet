# Resolve Entities and Scene Counts Workflow

Run:

```bash
python python_sdk/examples/02_ontology_management/02_resolve_entities_and_scene_counts.py
```

Covers:
- list ontology instances
- aggregate scene counts
- resolve entity instance ids
